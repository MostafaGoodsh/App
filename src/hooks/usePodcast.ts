// Re-export from context for backward compat
export { usePodcastContext as usePodcast } from '@/contexts/PodcastContext';
export type { PodcastEpisode, PlaylistTrack } from '@/contexts/PodcastContext';
